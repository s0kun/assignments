package analysis

import org.apache.spark.sql.SparkSession

object analyse {
  def apply(spark: SparkSession, flag: Int = 0) = {
    flag match {
      case 0 => {}
      case 1 => {}
      case 2 => {}
      case 3 => {}
    }

  }

}
