# Given an integer-array A of size N, calculate, for all
# sub-arrays of A, the number of distinct sum of elements.

# NOTE:
    # 1 <= N <= 10**5
    # 1 <= A[i] <= 10**5

n = int(input())
a = list(map(int,input().split()))

# 
